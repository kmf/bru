{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment_bru": "bru theme hints for Claude Code · https://github.com/kmf/bru — Claude Code's built-in theme list is limited, so this file picks the closest built-in (dark / light) and ensures truecolor rendering. When Claude Code ships user-configurable color themes (tracking issue #25366) this file will be extended with full bru palette bindings.",

  "theme": "dark",

  "env": {
    "COLORTERM": "truecolor",
    "BRU_VARIANT": "espresso"
  },

  "statusLine": {
    "type": "command",
    "command": "~/.claude/bru-statusline.sh",
    "padding": 0
  }
}
